import { GtmEventDirective } from './gtm-event.directive';

describe('GtmEventDirective', () => {
  it('should create an instance', () => {
    const directive = new GtmEventDirective();
    expect(directive).toBeTruthy();
  });
});
